{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "utest",
            "type": "shell",
            "command": "docker-compose up --build utest",
            "isBackground": true,
        },
        {
            "label": "lint",
            "type": "shell",
            "command": "docker-compose up --build lint",
            "isBackground": true,
        }
    ]
}